flask>=3.0.0
gunicorn>=21.2.0
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
plotly>=5.20.0
joblib>=1.3.0

# ── Standard-library modules (no install needed — built into Python) ──────────
# os          — file paths, makedirs, environ
# json        — JSON serialisation
# threading   — thread-safe bootstrap lock for Gunicorn workers
# glob        — stale .pyc cleanup on startup
# warnings    — imported locally inside train_model() only
